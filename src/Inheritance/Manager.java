package Inheritance;

import java.math.BigDecimal;

public class Manager extends Employee {
    public Manager(String name, String surname, BigDecimal salary) {
        super(name, surname, salary);
    }
}
